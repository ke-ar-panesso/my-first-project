# Operclite
The final project of Operative Systems, it's an app that can stabilish a communication between sockets throw the TCP protocol. Has two modules, one for linux and other for windows, linux module is for server that send comsuption reports and respond commands for the Windows Module. It was made in Go, using Fyne.io's library as GUI.
